"""Tests for the sdv.models.utils module."""
from unittest.mock import patch

import numpy as np
import pandas as pd
import pytest

from sdv.tabular.ctgan import  GaussianCopula
  

	def test__update_metadata(self):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k
	    
	def _fit(self, data):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

	def _sample(self, num_rows):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

	def get_parameters(self, flatten=False):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

	def _prepare_sampled_covariance(self, covariance):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

	def _unflatten_gaussian_copula(self, model_parameters):
	    # METHOD: _update_metadata(self)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

	def test__set_parameters(self, parameters, unflatten=False):
            # METHOD: set_parameters(self, parameters, unflatten=False)
	    # VALIDATE:
	    #     * returned values
	    #     * check the case whete len(scores) < self.k

